module.exports = {
  name: 'transfer',
  description: 'Transfer decrypted files to SFTP by date',
  async run(params = {}) {
    // TODO: implement transfer job
  },
  validate(params = {}) {
    // TODO: validate params
    return { ok: true };
  }
};


